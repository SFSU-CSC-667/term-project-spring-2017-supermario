/* 
 * function will be called in situation:
 *   a player ready to play;
 *   game start a new play or a new round;
 *   a wild-draw4 card makes a player get four cards;
 *   a draw-2 card makes a player got two cards
 */

const draw = (msg, bundle) => {
  console.log('draw is under develop')
}

module.exports = draw
